name: AIFoundary Guardrail Scan

on:
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: LOLA0786/aifoundary-action@v1.2.1
        with:
          scan-path: .
          mode: warn
          github-token: ${{ secrets.GITHUB_TOKEN }}
